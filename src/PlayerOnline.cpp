#include "Player/PlayerOnline.h"
#include "httplib.h"
#include <string>

void PlayerOnline::make_move() {
  // std::string move; // TODO move to func 
  // httplib::Client client(ip, port);

  // int timeout = 60 * 5; // max 5 min na ruch
  // client.set_max_timeout(timeout);

  // auto res = client.Get("/move/" + move);

  // if (res) {
      
  // }
}